spring:
  application:
    name: desafio-votacao
  datasource:
    url: jdbc:h2:file:./data/votacao
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
